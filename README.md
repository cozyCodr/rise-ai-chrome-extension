# Rise AI Dummy Extension

Minimal Chrome extension that injects a floating action button into every page. Load the folder via `chrome://extensions` → "Load unpacked" to test the UI stub.
# rise-ai-chrome-extension
